// require the model
// const Goldendoodle = require ('../models/goldendoodle');

// module.exports and export your new action
module.exports = {
    index,
    
};

//define the new function (controller function)

function index(req, res) {
    res.render('goldendoodles/f2bb');
}
